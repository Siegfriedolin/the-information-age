security_council = {
	
	recognize_country = {
	    
		target_trigger = {
			exists = yes
			FROM = { has_idea = partially_recognized_state }
			FROM = { NOT = { original_tag = ISS } }
		}
		
		visible = {
	        OR = {
		        has_idea = idea_united_nations_security_council_member
			    has_idea = idea_united_nations_NP_security_council_member
		    }
		}
		
		available = {
			NOT = { has_war_with = FROM }
		}
		
		modifier = {
		}
		remove = {
		}
		
		complete_effect = {
            #placeholder
		}
		
		icon = united_nations
		
		fire_only_once = no
		
		cost = 50
		
		ai_will_do = {
			base = 0
		}
	}
}