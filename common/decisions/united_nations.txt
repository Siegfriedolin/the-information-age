security_council = {
	
	recognize_country = {
	    
		target_trigger = {
			exists = yes
			FROM = { has_idea = partially_recognized_state }
			FROM = { NOT = { has_idea = idea_focus_generic_jihad } }
		}
		
		visible = {
	        OR = {
		        has_idea = idea_united_nations_security_council_member
			    has_idea = idea_united_nations_NP_security_council_member
		    }
		}
		
		available = {
			NOT = { has_war_with = FROM }
		}
		
		modifier = {
		}
		remove = {
		}
		
		complete_effect = {
		    add_to_variable = { var = recognize_country value = 1 }
            FROM = { country_event = { id = united_nations.22 hours = 1 } } 
		}
		
		icon = united_nations
		
		fire_only_once = no
		
		cost = 50
		
		ai_will_do = {
			base = 0
		}
	}
}