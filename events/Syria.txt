add_namespace = syria

#Douma Gas Attack
news_event = {
	id = syria.1
	title = syria.1.t
	desc = syria.1.d
	picture = GFX_news_event_syria_gas
	
	major = yes
	
	trigger = {
		NOT = { has_global_flag = syrian_civil_war_over }
		FSA = { controls_state = 946 has_war_with = SYR }
		SYR = { has_government = nationalist }
		date > 2018.4.6
		date < 2019.1.1
	}
	
	option = {
		name = syria.1.o1
		trigger = { original_tag = SYR }
	}
	
	option = {
		name = syria.1.o2
		trigger = {
			OR = { original_tag = SOV original_tag = PER is_in_faction_with = SYR }
		}
	}
	
	option = {
		name = syria.1.o3
		trigger = {
			OR = { original_tag = HTS original_tag = FSA original_tag = ISS }
		}
	}
	
	option = {
		name = syria.1.o4
		trigger = {
			NOT = { original_tag = FSA original_tag = HTS original_tag = SYR original_tag = SOV original_tag = PER original_tag = ISS is_in_faction_with = SYR }
		}
	}
}